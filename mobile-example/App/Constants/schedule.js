export const REQUEST_TIME_OFF_STEPS = {
  SET_DAY: 'SetDay',
  SET_DATES: 'SetDates'
}

export const TRADE_SHIFT_STEPS = {
  SELECT_SHIFT: 'SelectShift',
  SELECT_TEAMMATES: 'SelectTeammates'
}

export const COVER_SHIFT_STEPS = {
  SELECT_SHIFT: 'SelectShift',
  SELECT_TEAMMATES: 'SelectTeammates'
}
